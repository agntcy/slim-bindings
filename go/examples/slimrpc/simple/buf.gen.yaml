version: v2
managed:
  enabled: true
plugins:
  # Generates standard .pb.go files
  - remote: buf.build/protocolbuffers/go
    out: types
    opt:
      - paths=source_relative

  # Generates _slimrpc.pb.go files
  # Requires `protoc-gen-slimrpc-go` on PATH (built from the in-repo slimrpc-compiler by `task generate-proto`).
  - local: protoc-gen-slimrpc-go
    out: types
    opt:
      - paths=source_relative
