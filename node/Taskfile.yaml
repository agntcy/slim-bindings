# Copyright AGNTCY Contributors (https://github.com/agntcy)
# SPDX-License-Identifier: Apache-2.0

version: '3'

includes:
  adapter:
    taskfile: "../rust/Taskfile.yaml"
    internal: false
  rust:
    taskfile: ../tasks/rust.yaml
    internal: true
  tools:
    taskfile: ../tasks/tools.yaml
    internal: true
    flatten: true

vars:
  BINDINGS_DIR: "{{.TASKFILE_DIR}}/../rust"
  PROFILE: 'release'
  CURRENT_TARGET:
    sh: 'rustc -vV | sed -n "s|host: ||p"'
  TARGET: "{{.TARGET | default .CURRENT_TARGET}}"
  LIB_NAME: slim_bindings
  PKG_VERSION:
    sh: node -p "require('./package.json').version"
    dir: "{{.TASKFILE_DIR}}"
  DYLIB_PREFIX:
    sh: |
      case "$(uname -s)" in
        Darwin|Linux) echo "lib" ;;
        MINGW*|MSYS*|CYGWIN*) echo "" ;;
        *) echo "lib" ;;
      esac
  DYLIB_EXT:
    sh: |
      case "$(uname -s)" in
        Darwin) echo "dylib" ;;
        Linux) echo "so" ;;
        MINGW*|MSYS*|CYGWIN*) echo "dll" ;;
        *) echo "so" ;;
      esac

tasks:
  default:
    cmds:
      - task -l

  install-uniffi-bindgen-react-native:
    desc: Install uniffi-bindgen-react-native (napi target, >= 0.31.0-3) via npm devDependency
    dir: "{{.TASKFILE_DIR}}"
    status:
      - test -x "{{.TASKFILE_DIR}}/node_modules/.bin/ubrn"
    cmds:
      - echo "📦 Installing uniffi-bindgen-react-native (napi target)..."
      - npm install
    silent: true

  # Only builds when library is missing (CI places it from artifact; local dev builds).
  ensure-bindings-lib:
    desc: Build Rust bindings lib if not already in target release dir
    dir: "{{.TASKFILE_DIR}}"
    status:
      - sh -c 'find "{{.TASKFILE_DIR}}/../rust/target/{{.TARGET}}/release" -maxdepth 1 -name "*slim_bindings*" -type f 2>/dev/null | grep -q .'
    cmds:
      # Use CLI invocation so PROFILE=release propagates (task: vars can drop through nested includes)
      - task adapter:bindings:build:all TARGET={{.TARGET}} PROFILE=release

  generate:
    desc: Generate Node.js bindings using uniffi-bindgen-react-native (napi target)
    dir: "{{.TASKFILE_DIR}}"
    deps:
      - task: ensure-bindings-lib
        vars:
          TARGET: "{{.TARGET}}"
      - task: install-uniffi-bindgen-react-native
    cmds:
      - echo "🔧 Generating Node.js bindings (napi)..."
      - |
        # Compute library path with release profile.
        # DYLIB_EXT/DYLIB_PREFIX must be derived from TARGET (not host) so CI on Linux finds .dylib for darwin artifacts.
        PROFILE="release"
        TARGET="{{.TARGET}}"
        TASKFILE_DIR="{{.TASKFILE_DIR}}"
        LIB_NAME="{{.LIB_NAME}}"
        RELEASE_DIR="${TASKFILE_DIR}/../rust/target/${TARGET}/${PROFILE}"

        case "$TARGET" in
          *-apple-darwin)  DYLIB_EXT="dylib"; DYLIB_PREFIX="lib" ;;
          *-linux-*)       DYLIB_EXT="so";    DYLIB_PREFIX="lib" ;;
          *-windows-*|*-msvc) DYLIB_EXT="dll"; DYLIB_PREFIX="" ;;
          *)               DYLIB_EXT="so";    DYLIB_PREFIX="lib" ;;
        esac

        DEFAULT_LIB="${DYLIB_PREFIX}${LIB_NAME}.${DYLIB_EXT}"

        # Try target/TARGET/release first; when TARGET equals host, Cargo may use target/release/
        HOST_RELEASE_DIR="${TASKFILE_DIR}/../rust/target/${PROFILE}"
        if [ -f "${RELEASE_DIR}/${DEFAULT_LIB}" ]; then
          DYLIB_PATH="${RELEASE_DIR}/${DEFAULT_LIB}"
        elif [ -f "${HOST_RELEASE_DIR}/${DEFAULT_LIB}" ]; then
          DYLIB_PATH="${HOST_RELEASE_DIR}/${DEFAULT_LIB}"
        else
          echo "❌ Error: Library ${DEFAULT_LIB} not found in ${RELEASE_DIR} or ${HOST_RELEASE_DIR}"
          ls -la "${RELEASE_DIR}/" 2>/dev/null | grep slim || true
          ls -la "${HOST_RELEASE_DIR}/" 2>/dev/null | grep slim || true
          exit 1
        fi

        echo "   Library path: ${DYLIB_PATH}"
        echo "${DYLIB_PATH}" > /tmp/slim-node-dylib-path.txt
      - mkdir -p generated
      - |
        # Generate Node.js (napi) bindings with uniffi-bindgen-react-native.
        # Must run with the Rust crate dir as cwd (ubrn shells out to `cargo metadata`).
        # --lib-colocated bakes in "look for the native lib next to the compiled .js
        # file at runtime" - this matches our packaging model exactly: each
        # @agntcy/slim-bindings-<platform> tarball is a single self-contained
        # directory holding one platform's dylib alongside its own copy of the
        # generated JS (see pack-platform.ts), so no cross-package npm resolution
        # is needed at runtime.
        DYLIB_PATH=$(cat /tmp/slim-node-dylib-path.txt)
        OUT_DIR="{{.TASKFILE_DIR}}/generated"
        UBRN="{{.TASKFILE_DIR}}/node_modules/.bin/ubrn"
        cd "{{.BINDINGS_DIR}}"
        "$UBRN" generate napi bindings \
          --library --lib-colocated \
          --ts-dir "${OUT_DIR}" \
          "${DYLIB_PATH}"
      - |
        # Copy the dynamic library to the generated directory (generated code expects libslim_bindings.<ext>).
        DYLIB_PATH=$(cat /tmp/slim-node-dylib-path.txt)
        LIB_NAME="{{.LIB_NAME}}"
        TARGET="{{.TARGET}}"
        case "$TARGET" in
          *-apple-darwin)  DYLIB_EXT="dylib"; DYLIB_PREFIX="lib" ;;
          *-linux-*)       DYLIB_EXT="so";    DYLIB_PREFIX="lib" ;;
          *-windows-*|*-msvc) DYLIB_EXT="dll"; DYLIB_PREFIX="" ;;
          *)               DYLIB_EXT="so";    DYLIB_PREFIX="lib" ;;
        esac
        CANONICAL_NAME="${DYLIB_PREFIX}${LIB_NAME}.${DYLIB_EXT}"
        echo "📦 Copying dynamic library to generated/${CANONICAL_NAME}..."
        cp "${DYLIB_PATH}" "generated/${CANONICAL_NAME}"
        echo "✅ Copied to generated/${CANONICAL_NAME}"
        rm -f /tmp/slim-node-dylib-path.txt
      - |
        # No post-generation patching needed: uniffi-bindgen-react-native's napi
        # target emits directly runnable output against @ubjs/core + @ubjs/node
        # (unlike the retired uniffi-bindgen-node + ffi-rs pipeline).
        node -e "
          const fs = require('fs');
          const pkg = {
            name: '@agntcy/slim-bindings-generated',
            private: true,
            type: 'module',
            dependencies: { '@ubjs/core': '0.31.0-3', '@ubjs/node': '0.31.0-3' }
          };
          fs.writeFileSync('{{.TASKFILE_DIR}}/generated/package.json', JSON.stringify(pkg, null, 2) + '\n');
        "
      - echo "✅ Node.js bindings generated in generated/"
    silent: false

  install:
    desc: Install dependencies for generated bindings
    deps:
      - task: generate
    dir: "{{.TASKFILE_DIR}}/generated"
    cmds:
      - echo "📦 Installing generated bindings dependencies..."
      - npm install
      - echo "✅ Generated bindings dependencies installed"
    silent: true

  install:examples:
    desc: Install dependencies for examples
    deps:
      - task: install
    dir: "{{.TASKFILE_DIR}}/examples"
    cmds:
      - echo "📦 Installing examples dependencies..."
      - npm install
      - echo "✅ Examples dependencies installed"
    silent: true

  clean:
    desc: Clean build artifacts and node_modules
    cmds:
      - echo "🧹 Cleaning build artifacts..."
      - rm -rf generated/
      - rm -rf types/
      - rm -rf .platform-pkg/
      - rm -rf dist/
      - rm -rf examples/node_modules/
      - echo "✅ Clean complete"
    silent: true

  # Emit TypeScript declarations from generated/ to types/ (for main package publish).
  # noEmitOnError: false and verify output so emit succeeds as long as types/ is produced.
  emit-types:
    desc: Emit .d.ts from generated/ to types/ (run after generate)
    cmds:
      - test -d {{.TASKFILE_DIR}}/generated || (echo "Run task generate first"; exit 1)
      - npx -p typescript tsc -p {{.TASKFILE_DIR}}/tsconfig.emit-types.json || true
      - test -f {{.TASKFILE_DIR}}/types/slim_bindings.d.ts || (echo "No types emitted; fix tsconfig or generated code"; exit 1)
      - echo "✅ Types emitted to types/"
    silent: true

  # Pack a platform-specific npm package (for CI publish).
  # Requires: Rust lib built for TARGET. Produces dist/node-<platform>.tgz.
  pack:platform:
    desc: Pack platform-specific npm package (TARGET=, VERSION= optional)
    vars:
      VERSION: '{{.VERSION | default .PKG_VERSION}}'
    deps:
      - task: generate
        vars:
          TARGET: "{{.TARGET}}"
    cmds:
      - mkdir -p dist
      - npx tsx {{.TASKFILE_DIR}}/scripts/pack-platform.ts "{{.TARGET}}" "{{.VERSION}}"
    silent: false

  # Pack using pre-generated bindings (CI: generate once, then pack per target).
  # Requires: generated/ exists (from generate-node-bindings-once job); library in ../rust/target/TARGET/release.
  pack:platform:from-artifacts:
    desc: Pack platform package using existing generated/ and target library (no generate)
    dir: "{{.TASKFILE_DIR}}"
    vars:
      VERSION: '{{.VERSION | default .PKG_VERSION}}'
    cmds:
      - |
        TARGET="{{.TARGET}}"
        TASKFILE_DIR="{{.TASKFILE_DIR}}"
        LIB_NAME="{{.LIB_NAME}}"
        RELEASE_DIR="${TASKFILE_DIR}/../rust/target/${TARGET}/release"
        GEN_DIR="${TASKFILE_DIR}/generated"
        test -d "$GEN_DIR" || { echo "generated/ not found. Run generate-node-bindings-once job first."; exit 1; }
        case "$TARGET" in
          *-apple-darwin)  DYLIB_EXT="dylib"; DYLIB_PREFIX="lib" ;;
          *-linux-*)       DYLIB_EXT="so";    DYLIB_PREFIX="lib" ;;
          *-windows-*|*-msvc) DYLIB_EXT="dll"; DYLIB_PREFIX="" ;;
          *)               DYLIB_EXT="so";    DYLIB_PREFIX="lib" ;;
        esac
        CANONICAL="${DYLIB_PREFIX}${LIB_NAME}.${DYLIB_EXT}"
        SRC=""
        if [ -f "$RELEASE_DIR/$CANONICAL" ]; then
          SRC="$RELEASE_DIR/$CANONICAL"
        else
          count=0
          first=""
          case "$TARGET" in
            *-apple-darwin) _glob="$RELEASE_DIR"/*.dylib ;;
            *-linux-*) _glob="$RELEASE_DIR"/*.so ;;
            *-windows-*|*-msvc) _glob="$RELEASE_DIR"/*.dll ;;
            *) _glob="$RELEASE_DIR"/*.so ;;
          esac
          for f in $_glob; do
            [ -f "$f" ] || continue
            count=$((count + 1))
            first="$f"
          done
          if [ "$count" -eq 1 ]; then
            SRC="$first"
          elif [ "$count" -gt 1 ]; then
            echo "Multiple dynamic libraries in $RELEASE_DIR for $TARGET; refusing to guess:"
            ls -la "$RELEASE_DIR"
            exit 1
          fi
        fi
        if [ -z "$SRC" ] || [ ! -f "$SRC" ]; then
          echo "No dynamic library found for $TARGET (expected $CANONICAL) in $RELEASE_DIR"
          ls -la "$RELEASE_DIR/" 2>/dev/null || true
          exit 1
        fi
        cp -v "$SRC" "$GEN_DIR/$CANONICAL"
      - mkdir -p dist
      - npx tsx {{.TASKFILE_DIR}}/scripts/pack-platform.ts "{{.TARGET}}" "{{.VERSION}}"
    silent: false

  # Examples
  example:server:
    desc: Run the SLIM Node.js server
    deps:
      - task: install:examples
    dir: "{{.TASKFILE_DIR}}/examples"
    cmds:
      - echo "🚀 Starting SLIM Node.js server..."
      - npm run server -- {{.CLI_ARGS}}
    silent: false

  example:alice:
    desc: Run Alice (receiver) for point-to-point example
    aliases: [example:p2p:alice]
    deps:
      ## - task: install:examples
    dir: "{{.TASKFILE_DIR}}/examples"
    cmds:
      - echo "👩 Starting Alice (receiver)..."
      - npm run alice -- {{.CLI_ARGS}}
    silent: false

  example:bob:
    desc: Run Bob (sender) for point-to-point example
    aliases: [example:p2p:bob]
    deps:
      ## - task: install:examples
    dir: "{{.TASKFILE_DIR}}/examples"
    cmds:
      - echo "👨 Starting Bob (sender)..."
      - npm run bob -- {{.CLI_ARGS}}
    silent: false

  example:group:
    desc: Run the group messaging example (moderator or participant, see --help)
    deps:
      - task: install:examples
    dir: "{{.TASKFILE_DIR}}/examples"
    cmds:
      - echo "👥 Starting group example..."
      - npm run group -- {{.CLI_ARGS}}
    silent: false

  # SlimRPC (protoc-gen-slimrpc-node) proto generation + example
  generate-proto:
    desc: "Generate slimrpc + protobuf-es TypeScript for the Node.js examples"
    deps:
      - task: rust:run-command
        vars:
          COMMAND: |
            cargo install --locked agntcy-protoc-slimrpc-plugin --bin protoc-gen-slimrpc-node
      - task: buf
    cmds:
      - |
        # Find every buf.gen.yaml under node/ and run buf generate in its folder.
        # `protoc-gen-slimrpc-node` must be on PATH (installed above into ~/.cargo/bin).
        cd "{{.TASKFILE_DIR}}"
        find . -name 'buf.gen.yaml' | while read -r file; do
          echo "Generating proto files for $file"
          folder=$(dirname "$file")
          pushd > /dev/null "${folder}" && \
            PATH="${HOME}/.cargo/bin:${PATH}" {{.TOOLS_INSTALL_DIR}}/buf generate && \
            popd > /dev/null
        done

  example:slimrpc:install:
    desc: Install dependencies for the SlimRPC simple example
    deps:
      - task: install
    dir: "{{.TASKFILE_DIR}}/examples/slimrpc/simple"
    cmds:
      - echo "📦 Installing SlimRPC example dependencies..."
      - npm install
    silent: true

  example:slimrpc:server:
    desc: Run the SlimRPC example server (use --instance server1/server2 for group)
    deps:
      - task: example:slimrpc:install
    dir: "{{.TASKFILE_DIR}}/examples/slimrpc/simple"
    cmds:
      - echo "🚀 Starting SlimRPC server..."
      - npm run server -- {{.CLI_ARGS}}
    silent: false

  example:slimrpc:client:
    desc: Run the SlimRPC point-to-point example client
    deps:
      - task: example:slimrpc:install
    dir: "{{.TASKFILE_DIR}}/examples/slimrpc/simple"
    cmds:
      - echo "📞 Starting SlimRPC client..."
      - npm run client -- {{.CLI_ARGS}}
    silent: false

  example:slimrpc:client-group:
    desc: Run the SlimRPC multicast (group) example client
    deps:
      - task: example:slimrpc:install
    dir: "{{.TASKFILE_DIR}}/examples/slimrpc/simple"
    cmds:
      - echo "📢 Starting SlimRPC group client..."
      - npm run client-group -- {{.CLI_ARGS}}
    silent: false

  # Tests
  install:tests:
    desc: Install dependencies for tests
    deps:
      - task: install
    dir: "{{.TASKFILE_DIR}}/tests"
    cmds:
      - echo "📦 Installing test dependencies..."
      - npm install
      - echo "✅ Test dependencies installed"
    silent: true

  test:integration:
    desc: Run integration tests for Node.js bindings
    deps:
      - task: install:tests
    dir: "{{.TASKFILE_DIR}}/tests"
    cmds:
      - echo "🧪 Running integration tests..."
      - npm test
    silent: false
