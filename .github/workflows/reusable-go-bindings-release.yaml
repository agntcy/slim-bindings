# Copyright AGNTCY Contributors (https://github.com/agntcy)
# SPDX-License-Identifier: Apache-2.0

---
name: reusable-go-bindings-release

on:
  workflow_call:
    inputs:
      library-name:
        description: Name of the library
        required: false
        type: string
        default: slim_bindings
      go-bindings-repo:
        description: Target repository for Go bindings (org/repo format)
        required: false
        type: string
        default: agntcy/slim-bindings-go
      go-version:
        description: Go version to use
        required: false
        type: string
        default: '1.25'
      go-bindings-working-directory:
        description: Working directory for Go bindings
        required: false
        type: string
        default: ./go
    secrets:
      gh-token:
        description: GitHub token for pushing to target repository
        required: true

env:
  LIBRARY_NAME: ${{ inputs.library-name }}
  GO_BINDINGS_REPO: ${{ inputs.go-bindings-repo }}
  FILES_TO_RELEASE: cmd slimrpc go.mod go.sum LICENSE README.md SLIMRPC.md slim_bindings.go slim_bindings.h

jobs:
  # ==========================================================================
  # Job 1: Generate and publish Go bindings to pre-release branch
  # ==========================================================================
  release-go-bindings:
    name: Release Go Bindings
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
        working-directory: ${{ inputs.go-bindings-working-directory }}
    steps:
      - name: Checkout source repo
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Setup Go
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version: ${{ inputs.go-version }}

      - name: Setup Rust
        uses: ./.github/actions/setup-rust
        with:
          workspace: ./rust
          cache-enabled: false

      # Download all library artifacts
      - name: Download Generated Go Binding Artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          merge-multiple: true
          path: ${{ inputs.go-bindings-working-directory }}/downloaded_slim_bindings

      - name: List downloaded go binding artifacts
        run: |
          echo "📦 Downloaded artifacts:"
          find ./downloaded_slim_bindings -type f -ls

      - name: Clone target repository
        env:
          GH_TOKEN: ${{ secrets.gh-token }}
        run: |
          git clone "https://x-access-token:${GH_TOKEN}@github.com/${GO_BINDINGS_REPO}.git" ./target-repo

      - name: Update target repository
        run: |
          pushd ./target-repo

          # Copy Go bindings
          cp -r ../downloaded_slim_bindings/* ./

          # Show what will be committed
          echo "📋 Files to be committed:"
          git status

          popd

      - name: Commit and push to pre-release branch
        env:
          REF_NAME: ${{ github.ref_name }}
          REPO: ${{ github.repository }}
          SHA: ${{ github.sha }}
        run: |
          pushd ./target-repo

          # Extract version from tag name (remove 'slim-bindings-' prefix)
          VERSION="${REF_NAME}"
          VERSION="${VERSION#slim-bindings-}"
          echo "📦 Extracted version: $VERSION"

          # Go requires a /vN suffix in the module path at major version >= 2.
          # Source keeps the unsuffixed path for local dev; rewrite only here.
          MAJOR="${VERSION#v}"; MAJOR="${MAJOR%%.*}"
          case "$MAJOR" in ''|*[!0-9]*) MAJOR=0 ;; esac
          if [ "$MAJOR" -ge 2 ]; then
            echo "🔧 Rewriting Go module path to /v${MAJOR}"
            find . -type f \( -name '*.go' -o -name 'go.mod' -o -name '*.md' \) -print0 \
              | xargs -0 sed -i "s|github.com/${GO_BINDINGS_REPO}|github.com/${GO_BINDINGS_REPO}/v${MAJOR}|g"

            # The /vN suffix adds one directory level to the module's on-disk
            # location in the consumer's cache, so the cgo LDFLAGS that reach
            # $GOPATH/.cgo-cache via ${SRCDIR} need one extra '../' to resolve.
            find . -type f -name '*.go' -print0 \
              | xargs -0 sed -i 's#/\.\./\.\./\.\./\.\./\.\./\.cgo-cache#/../../../../../../.cgo-cache#g'
          fi

          # Create pre-release branch name
          PRE_RELEASE_BRANCH="pre-release-$VERSION"
          echo "🌿 Pre-release branch: $PRE_RELEASE_BRANCH"

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          read -ra files <<< "$FILES_TO_RELEASE"
          git add "${files[@]}"

          # Check if there are changes to commit
          if git diff --cached --quiet; then
            echo "✅ No changes to commit - bindings are up-to-date"
            exit 0
          fi

          git commit -m "Release $VERSION

          Generated from https://github.com/${REPO}/commit/${SHA}

          Platforms:
          - linux/amd64 (x86_64-unknown-linux-gnu)
          - linux/arm64 (aarch64-unknown-linux-gnu)
          - linux/amd64-musl (x86_64-unknown-linux-musl)
          - linux/arm64-musl (aarch64-unknown-linux-musl)
          - darwin/arm64 (aarch64-apple-darwin)
          - darwin/amd64 (x86_64-apple-darwin)
          - windows/amd64 (x86_64-pc-windows-msvc)"

          # Create pre-release tag so Go can resolve by version (not pseudo-version)
          git tag -a "$VERSION" -m "Pre-release $VERSION"

          # Create and push pre-release branch and tag
          git checkout -b "$PRE_RELEASE_BRANCH"
          git push origin "$PRE_RELEASE_BRANCH"
          git push origin "$VERSION"

          echo "✅ Successfully pushed Go bindings to pre-release branch: $PRE_RELEASE_BRANCH"
          echo "✅ Created pre-release tag: $VERSION"

          popd

  # ==========================================================================
  # Job 2: Test the published Go bindings work as expected
  # ==========================================================================
  test-go-bindings:
    name: Test Go Bindings (${{ matrix.platform.target }})
    runs-on: ${{ matrix.platform.runner }}
    needs: release-go-bindings
    if: startsWith(github.ref, 'refs/tags/slim-bindings-')
    strategy:
      fail-fast: false
      matrix:
        platform:
          # Linux x86_64 variants
          - runner: ubuntu-24.04
            os: linux
            target: x86_64-unknown-linux-gnu
          - runner: ubuntu-24.04
            os: linux
            target: x86_64-unknown-linux-musl
          # Linux ARM64 variants (using QEMU emulation)
          - runner: ubuntu-24.04
            os: linux
            target: aarch64-unknown-linux-gnu
            use_qemu: true
          - runner: ubuntu-24.04
            os: linux
            target: aarch64-unknown-linux-musl
            use_qemu: true
          # Windows variants
          - runner: windows-latest
            os: windows
            target: x86_64-pc-windows-msvc
          - runner: windows-latest
            os: windows
            target: x86_64-pc-windows-gnu
          # macOS variants
          - runner: macos-15-intel
            os: macos
            target: x86_64-apple-darwin
          - runner: macos-15
            os: macos
            target: aarch64-apple-darwin
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Setup QEMU for ARM64 emulation
        if: matrix.platform.use_qemu == true
        uses: docker/setup-qemu-action@ce360397dd3f832beb865e1373c09c0e9f86d70a # v4.0.0
        with:
          platforms: arm64

      - name: Setup Go
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version: ${{ inputs.go-version }}

      - name: Wait for GitHub to update (package propagation)
        run: |
          echo "⏳ Waiting 60 seconds for GitHub package index to update..."
          sleep 60

      - name: Test bindings installation and usage (ARM64 with QEMU)
        if: matrix.platform.use_qemu == true
        env:
          REF_NAME: ${{ github.ref_name }}
          PLATFORM_TARGET: ${{ matrix.platform.target }}
          GO_BINDINGS_REPO_NAME: ${{ env.GO_BINDINGS_REPO }}
        run: |
          # Extract version from tag name
          VERSION="${REF_NAME}"
          VERSION="${VERSION#slim-bindings-}"
          echo "📦 Testing version: $VERSION"
          echo "🖥️  Platform: $PLATFORM_TARGET"
          echo "🔧 Using QEMU ARM64 emulation"

          # Module path mirrors the release job: /vN suffix at major >= 2.
          MAJOR="${VERSION#v}"; MAJOR="${MAJOR%%.*}"
          case "$MAJOR" in ''|*[!0-9]*) MAJOR=0 ;; esac
          MODULE_PATH="github.com/${GO_BINDINGS_REPO_NAME}"
          [ "$MAJOR" -ge 2 ] && MODULE_PATH="${MODULE_PATH}/v${MAJOR}"

          # Determine Go arch for ARM64
          if [[ "$PLATFORM_TARGET" == *"aarch64"* ]]; then
            export GO_ARCH="arm64"
          else
            export GO_ARCH="amd64"
          fi

          # Determine libc variant
          if [[ "$PLATFORM_TARGET" == *"musl"* ]]; then
            ALPINE_VARIANT="alpine:latest"
          else
            ALPINE_VARIANT="arm64v8/ubuntu:22.04"
          fi

          # Run test in ARM64 container
          docker run --rm --platform linux/arm64 \
            -v "$(pwd)":/workspace \
            -v "$(pwd)":/repo:ro \
            -w /workspace \
            "$ALPINE_VARIANT" \
            sh -c "
              # Install dependencies
              if command -v apk >/dev/null; then
                apk add --no-cache go git
              else
                apt-get update && apt-get install -y golang git
              fi

              # Create test directory
              TEST_DIR=\$(mktemp -d)
              cd \$TEST_DIR
              echo '📂 Testing in: '\$TEST_DIR

              # Initialize test module
              go mod init test
              echo '✅ Created test module'

              # Fetch bindings
              go get ${MODULE_PATH}@$VERSION
              echo '✅ Fetched bindings package version $VERSION'

              # Run setup tool
              go run ${MODULE_PATH}/cmd/slim-bindings-setup@$VERSION
              echo '✅ Downloaded native libraries'

              # Copy test program and point its import at the /v2 module path.
              cp /repo/.github/tests/go/test_installation.go main.go
              sed -i \"s|github.com/${GO_BINDINGS_REPO_NAME}|${MODULE_PATH}|g\" main.go

              # Run test
              go run main.go

              echo ''
              echo '🎉 All tests passed on ${PLATFORM_TARGET}!'
            "

      - name: Test bindings installation and usage (Native)
        if: matrix.platform.use_qemu != true
        env:
          REF_NAME: ${{ github.ref_name }}
          PLATFORM_TARGET: ${{ matrix.platform.target }}
          PLATFORM_RUNNER: ${{ matrix.platform.runner }}
          GO_BINDINGS_REPO_NAME: ${{ env.GO_BINDINGS_REPO }}
        run: |
          # Extract version from tag name
          VERSION="${REF_NAME}"
          VERSION="${VERSION#slim-bindings-}"
          echo "📦 Testing version: $VERSION"

          # Module path mirrors the release job: /vN suffix at major >= 2.
          MAJOR="${VERSION#v}"; MAJOR="${MAJOR%%.*}"
          case "$MAJOR" in ''|*[!0-9]*) MAJOR=0 ;; esac
          MODULE_PATH="github.com/${GO_BINDINGS_REPO_NAME}"
          [ "$MAJOR" -ge 2 ] && MODULE_PATH="${MODULE_PATH}/v${MAJOR}"

          # Create a temporary test directory
          if [ "$RUNNER_OS" = "Windows" ]; then
            TEST_DIR=$(mktemp -d -p "$RUNNER_TEMP")
          else
            TEST_DIR=$(mktemp -d)
          fi
          cd "$TEST_DIR"
          echo "📂 Testing in: $TEST_DIR"
          echo "🖥️  Platform: $PLATFORM_TARGET"
          echo "🏃 Runner: $PLATFORM_RUNNER"

          # For Windows MinGW, we need to ensure MinGW tools are in PATH
          if [[ "$PLATFORM_TARGET" == "x86_64-pc-windows-gnu" ]]; then
            echo "🔧 Setting up MinGW environment"
            # GitHub Windows runners have MinGW installed
            export PATH="/c/ProgramData/chocolatey/lib/mingw/tools/install/mingw64/bin:$PATH"
            gcc --version || echo "⚠️  MinGW GCC not found in PATH"
          fi

          # Initialize a test Go module
          go mod init test
          echo "✅ Created test module"

          # Fetch the bindings using the version tag
          go get "${MODULE_PATH}@$VERSION"
          echo "✅ Fetched bindings package version $VERSION"

          # Run the setup tool to download native libraries
          go run "${MODULE_PATH}/cmd/slim-bindings-setup@$VERSION"
          echo "✅ Downloaded native libraries"

          # Copy test program and point its import at the /v2 module path
          # (.bak suffix keeps sed portable across BSD/macOS and GNU).
          cp "$GITHUB_WORKSPACE/.github/tests/go/test_installation.go" main.go
          sed -i.bak "s|github.com/${GO_BINDINGS_REPO_NAME}|${MODULE_PATH}|g" main.go && rm -f main.go.bak

          # Run the test program
          go run main.go

          echo ""
          echo "🎉 All tests passed on $PLATFORM_TARGET!"

      - name: Test result summary
        if: success()
        env:
          PLATFORM_TARGET: ${{ matrix.platform.target }}
        run: |
          echo "✅ Go bindings verification completed successfully on $PLATFORM_TARGET"
          echo "The published bindings can be safely tagged for release."

  # ==========================================================================
  # Job 3: Finalize release - merge to main, tag, and cleanup
  # ==========================================================================
  finalize-go-release:
    name: Finalize Release
    runs-on: ubuntu-latest
    needs: [release-go-bindings, test-go-bindings]
    if: startsWith(github.ref, 'refs/tags/slim-bindings-')
    steps:
      - name: Clone target repository
        env:
          GH_TOKEN: ${{ secrets.gh-token }}
        run: |
          git clone "https://x-access-token:${GH_TOKEN}@github.com/${GO_BINDINGS_REPO}.git" ./target-repo

      - name: Merge to main and create tag
        env:
          REF_NAME: ${{ github.ref_name }}
        run: |
          cd ./target-repo

          # Set git config locally
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # Extract version from tag name
          VERSION="${REF_NAME}"
          VERSION="${VERSION#slim-bindings-}"
          PRE_RELEASE_BRANCH="pre-release-$VERSION"

          echo "📦 Version: $VERSION"
          echo "🌿 Pre-release branch: $PRE_RELEASE_BRANCH"

          # Fetch the pre-release branch
          git fetch origin "$PRE_RELEASE_BRANCH"

          # Checkout main and merge pre-release branch
          git checkout main
          git merge "origin/$PRE_RELEASE_BRANCH" --ff-only

          # Note: Version tag already exists from Job 1, so we force update it
          # to point to the merged commit on main
          git tag -f -a "$VERSION" -m "Release $VERSION"

          # Push main and tag
          git push origin main
          git push -f origin "$VERSION"

          echo "✅ Merged pre-release branch to main"
          echo "✅ Updated tag $VERSION to point to main"

      - name: Cleanup pre-release branch
        if: always() # Run even if previous steps fail
        env:
          REF_NAME: ${{ github.ref_name }}
          JOB_STATUS: ${{ job.status }}
        run: |
          cd ./target-repo

          # Extract version from tag name
          VERSION="${REF_NAME}"
          VERSION="${VERSION#slim-bindings-}"
          PRE_RELEASE_BRANCH="pre-release-$VERSION"

          echo "🧹 Cleaning up pre-release artifacts..."

          # Delete the pre-release branch
          if git ls-remote --heads origin "$PRE_RELEASE_BRANCH" | grep -q "$PRE_RELEASE_BRANCH"; then
            git push origin --delete "$PRE_RELEASE_BRANCH" && echo "✅ Deleted pre-release branch: $PRE_RELEASE_BRANCH" || echo "⚠️  Failed to delete branch"
          else
            echo "ℹ️  Pre-release branch doesn't exist (may have been deleted already)"
          fi

          # If the merge step failed, we need to delete the tag too
          # (it's pointing to pre-release branch, not main)
          if [ "$JOB_STATUS" != "success" ]; then
            echo "⚠️  Job failed - cleaning up pre-release tag as well"
            if git ls-remote --tags origin "$VERSION" | grep -q "$VERSION"; then
              git push origin --delete "$VERSION" && echo "✅ Deleted pre-release tag: $VERSION" || echo "⚠️  Failed to delete tag"
            else
              echo "ℹ️  Tag doesn't exist (may have been deleted already)"
            fi
          fi

          echo "✅ Cleanup complete"

      - name: Release complete
        env:
          REF_NAME: ${{ github.ref_name }}
        run: |
          VERSION="${REF_NAME}"
          VERSION="${VERSION#slim-bindings-}"
          echo ""
          echo "🎉 Release $VERSION complete!"
          echo "✅ Go bindings published to https://github.com/${GO_BINDINGS_REPO}"
          echo "✅ Tagged as $VERSION"
          echo "✅ All tests passed on 8 platforms"
